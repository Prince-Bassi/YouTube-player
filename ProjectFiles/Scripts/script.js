import videoManager from "./videoManager.js";

// fetch("/getData", {
//        method: "POST",
//        headers: {
//               "Content-Type": "text/html"
//        },
//        body: id
// })
// .then(res => res.json())
// .then(resData => {
//        const videoData = resData.data.items[0];
//        player.innerHTML = videoData.player.embedHtml;
//        player.childNodes[0].classList.add("playerFrame");
//        player.childNodes[0].src = 
//        console.log(resData, videoData.player.embedHtml);
// })
// .catch(err => console.error(err));

document.addEventListener("keydown", (event) => {
       if (event.key === "k") {
              
       }
});